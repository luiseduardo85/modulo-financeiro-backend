# Local PostgreSQL connection settings (TECH-003).
# Copy this file to .env and adjust if needed. .env is gitignored and must
# never contain real/production credentials — these are local-dev-only values.
DB_HOST=localhost
DB_PORT=5432
DB_NAME=financeiro
DB_USER=financeiro
DB_PASSWORD=financeiro
