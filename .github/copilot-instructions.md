# Compatibility note

The repository's canonical coding-agent instructions are now in:

`/AGENTS.md`

Read and follow `AGENTS.md` first.

This file remains only for compatibility with GitHub Copilot and must not duplicate project governance.

The documentation under `docs/` remains the source of truth for business rules, domain behavior, architecture, database conventions, API contracts and Use Cases.
