spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:financeiro}
    username: ${DB_USER:financeiro}
    password: ${DB_PASSWORD:financeiro}

logging:
  level:
    root: INFO
    com.financeiro: DEBUG
  pattern:
    correlation: "[traceId=%X{traceId:-}] "
